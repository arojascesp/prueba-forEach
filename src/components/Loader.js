//TI imports
import React, { Fragment } from "react";

const Loader = () => {
  return (
    <Fragment>
      <h1 className="text-center">Loading...</h1>
    </Fragment>
  );
};

export default Loader;
